@extends('layouts.app') @section('content')<main class="container py-4"><h1>Fornecedores e prestadores</h1>@if(session('success'))<div class="alert alert-success">{{session('success')}}</div>@endif<form method="get" class="mb-3"><input name="q" value="{{request('q')}}" placeholder="Buscar fornecedor"><button>Buscar</button></form><form method="post" action="{{route('suppliers.store')}}" class="card p-3 mb-4">@csrf<h2>Novo fornecedor</h2><input name="trade_name" placeholder="Nome fantasia"><input name="legal_name" placeholder="Razão social"><input name="document" placeholder="CPF/CNPJ"><input name="aliases[]" placeholder="Alias (opcional)"><label><input type="checkbox" name="active" value="1" checked> Ativo</label><button>Cadastrar</button></form><table class="table"><thead><tr><th>Nome</th><th>Documento</th><th>Aliases</th><th>Status</th></tr></thead><tbody>@foreach($items as $s)<tr><td>{{ $s->displayName() }}<small>{{ $s->legal_name }}</small></td><td>{{ $s->formattedDocument() ?: '—' }}</td><td>{{ $s->aliases->pluck('alias')->join(', ') ?: '—' }}</td><td>{{ $s->active?'Ativo':'Inativo' }}</td></tr>@endforeach</tbody></table>{{ $items->links() }}</main>@endsection
